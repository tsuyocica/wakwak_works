<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow-sm p-4">
        <h2 class="mb-4 text-center">パスワードをお忘れですか？</h2>
        <p class="text-center text-muted mb-4">
          登録したメールアドレスを入力してください。<br>
          リセット用のリンクを送信します。
        </p>

        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, novalidate: true }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="mb-3">
            <%= f.label :email, "メールアドレス", class: "form-label fw-bold" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>

          <!-- ✅ ボタン -->
          <div class="d-flex justify-content-center mt-3">
            <%= f.submit "パスワードリセットのリンクを送信", class: "btn btn-primary px-4 py-2" %>
          </div>
        <% end %>

        <!-- ✅ リンク部分 -->
        <div class="text-center mt-3">
          <%= link_to "ログイン", new_user_session_path, class: "text-decoration-none d-block" %>
          <%= link_to "新規登録はこちら", new_user_registration_path, class: "text-decoration-none d-block mt-2" %>
        </div>
      </div>
    </div>
  </div>
</div>